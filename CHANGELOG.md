# 更新日志 (Changelog)

## v2.1.0 - Autumn Update（2026-09-13）

在 v2.0.0 基础上，本次更新主要为站点加入「访问量」与「稳定运行时间」统计，并对设置页做了一处视觉微调。

### ✨ 新增

- **设置页「访问量」板块**（位于「关于 → 鸣谢」下方，独立卡片）
  - 接入 **不蒜子（busuanzi）** 统计站点总访问量（PV）与访客数（UV），零后端、免费；
  - 文字颜色跟随系统主题：**深色模式白字 / 浅色模式黑字**，任意底色都清晰可读；
  - 部署到线上真实域名后自动填充真实访问量；本地 `localhost` 预览时数字显示「—」占位属正常现象（不蒜子对本地域名不统计）。

- **同板块新增「站点稳定运行时长」**
  - 自 **2026-08-29** 起算，精确到**秒**，每 1 秒自动走字；
  - 纯前端计算，**无外部依赖**，本地与线上均正常显示，不会出现空白。

### 🎨 优化 / 调整

- 设置页「节日皮肤」展开项高度与上方「主题」对齐（48px → 68px），描述文字不再显得拥挤；
- 访问量板块与上方「鸣谢」卡片的间距微调（最终定为 6px），视觉更协调；
- 访问量板块做成与鸣谢同款独立卡片（圆角 + 边框 + 跟随主题背景），整体更统一。

### 📦 工程

- 新增组件 `src/gallery/VisitorCounter.vue`；
- 访问量与运行时长逻辑集成于 `src/gallery/pages/SettingsPage.vue` 的「关于」区。

---

## v2.0.0 - Autumn（2026-09-11）

- 站点基础框架与软件数据上线（详见 `文字设置.ts` 中的版本说明）。
